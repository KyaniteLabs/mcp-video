# Frontier of Agentic Video — mid-2026 (Claude research agent)

KEY FINDINGS (full text in transcript):
1. Gen-video APIs: frontier moved from "generate a clip" to "converse with the clip". Gemini Omni Flash = default, multi-turn conversational video editing via Interactions API; Veo 3.1 relegated to legacy. Sora 2 Videos API DEPRECATED, shuts down 2026-09-24 → vendor video APIs churn ~12mo; abstraction layers win. Runway Gen-4 Aleph = in-context v2v editing. Kling 3.0 multi-shot narratives. Open weights local on 24GB GPU: Wan 2.2, LTX-2, HunyuanVideo 1.5.
2. Closed-loop editing REAL: Diffusion Studio agent (CodeAct + VisualFeedbackTool, VLM evaluates at 1fps, generator-evaluator loop; browser-engine-bound, MCP on roadmap). VideoAgent (TMLR 2025) VLM-as-verifier 4x improvement. Gemini video-in commodity (20GB files, timestamp QA). TwelveLabs Pegasus 2hr assets. GAP: nobody couples a DETERMINISTIC editor (FFmpeg-level) with a VLM critic loop as an open protocol — directly adjacent to mcp-video guardrails.
3. Semantic editing: Descript Underlord = agent layer over transcript paradigm ("Cursor for video"). Opus ClipAnything prompt-driven clipping + per-clip virality score. TwelveLabs ($100M B) NL retrieval over footage, ships official MCP server. OpenMontage, OpenReelio OSS. Thin spot: "make it punchier" directive→edit→measured improvement loop doesn't exist.
4. Standards: OpenTimelineIO = the obvious agent-legible EDL, nobody owns it for agents yet. MCP Tasks primitive (SEP-1686) on official 2026 roadmap = sanctioned pattern for long renders. MCP media space crowding: json2video MCP, Remotion official MCP, studiomeyer-io/mcp-video (NAME COLLISION with KyaniteLabs repo!), TwelveLabs MCP, Higgsfield MCP. No streaming/partial-render protocol standard yet — open territory.
5. Quality judgment: VQualA 2025 benchmark (engagement prediction). Opus virality score shipped/relied on. Honest read: content-only retention prediction works at "rank my clips" fidelity, NOT "this cut at 00:12 loses viewers". Grading an EDIT (vs finished video) = open research gap.

TABLE STAKES IN 12 MONTHS: (1) conversational multi-turn edit loops; (2) VLM self-review of rendered output ("guardrails today are static; tomorrow they watch the output"); (3) long jobs as MCP Tasks; (4) NL moment retrieval over local footage; (5) agent-legible timelines (OTIO as diff format — cheap to claim, unclaimed); (6) hybrid deterministic+generative with swappable adapters (vendor churn!); (7) per-clip retention scoring in export path.
